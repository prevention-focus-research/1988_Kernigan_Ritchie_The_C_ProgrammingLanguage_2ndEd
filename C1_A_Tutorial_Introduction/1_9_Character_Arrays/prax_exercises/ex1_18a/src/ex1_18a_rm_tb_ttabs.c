/* 1. remove trailing blanks from each line of input.*/

#include <stdio.h>

int main()
{
    int c;

    while ((c = getchar()) != EOF) {
        if (c != ' ' || c != '\t') {
            
        }
    }
}